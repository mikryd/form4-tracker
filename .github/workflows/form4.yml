name: Form4 Daily

on:
  schedule:
    - cron: "15 12 * * *"  # Runs daily at 12:15 UTC
  workflow_dispatch:

permissions:
  contents: write  # allows committing CSV updates

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install feedparser requests beautifulsoup4 pandas

      - name: Run SEC scraper
        run: python form4_to_csv.py

      - name: Commit and push updated CSVs
        uses: EndBug/add-and-commit@v9
        with:
          add: "*.csv"
          message: "Auto-update: daily Form 4 insider buys"
          default_author: github_actions